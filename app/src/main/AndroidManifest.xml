<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Cámara -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Compatibilidad <= 32 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Intents que vamos a usar (cámara / galería) -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filmoteca">

        <!-- Launcher: sigue siendo MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para la práctica de fragmentos -->
        <activity
            android:name=".FilmFragmentsActivity"
            android:label="Filmoteca (Fragments)"
            android:exported="false" />

        <!-- Parte 1 (XML/Views) -->
        <activity android:name=".AboutActivity" android:exported="false" />
        <activity android:name=".FilmDataActivity" android:exported="false" />
        <activity android:name=".FilmEditActivity" android:exported="false" />
        <activity android:name=".FilmDetailActivity" android:exported="false" />

        <!-- Parte 2: Listas -->
        <activity android:name=".FilmListActivity" android:exported="false" />
        <activity android:name=".FilmListCustomActivity" android:exported="false" />
        <activity android:name=".FilmListRvActivity" android:exported="false" />

        <!-- Compose -->
        <activity android:name=".FilmListComposeActivity" android:exported="false" />
        <activity android:name=".FilmDetailComposeActivity" android:exported="false" />
        <activity android:name=".FilmEditComposeActivity" android:exported="false" />

        <!-- FileProvider para las fotos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
